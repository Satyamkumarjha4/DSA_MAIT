#include <iostream>
using namespace std;


int main(){
    int n;
    
    cout<<"enter the length of array";
    cin>>n;
    int arr[n];
    
    for(int i=0; i<n; i++){
        cout<<"enter the number";
        cin>>arr[i];
    }
    cout<<"data entered sucessfully"<<endl;
    
    int c;
    cout<<"enter the number to be checked";
    cin>>c;
    for(int i =0; i<n; i++){
        if(c==arr[i]){
            cout<<"the number was found at index: "<<i<<endl;
            return 0;
        }
    }
    cout<<"the numbe rwas not found"<<endl;
    return 1;
}
